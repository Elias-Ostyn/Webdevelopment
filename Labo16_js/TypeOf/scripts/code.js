const setup = () => {

    let leeftijd = 34;          // Number
    let intrest = 0.12;         // Number
    let isGevaarlijk = true;    // Boolean
    let vandaag = new Date();   // Object (Date-object)
    const print = (message) => {
        console.log(message);
    }                           // Function
}
window.addEventListener("load", setup);
